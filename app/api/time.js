export default async function handler(request, response) {
  // const currentDate = new Date();
  // const dateInfo = currentDate.toLocaleDateString("ko-KR");
  // const timeInfo = currentDate.toLocaleTimeString("ko-KR", {
  //   hour: "2-digit",
  //   minute: "2-digit",
  // });
  // return response.status(200).json({ date: dateInfo, time: timeInfo });
}
